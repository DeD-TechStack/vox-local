# Core
PyQt6>=6.6.0
faster-whisper>=1.0.0
requests>=2.31.0
PyYAML>=6.0

# Audio
sounddevice>=0.4.6
numpy>=1.24.0

# Input / Control
# keyboard is used in two places:
#   - executor.py: media key simulation (mute, play/pause, next/prev track)
#   - listener.py: push-to-talk hotkey detection (push_to_talk activation mode only)
keyboard>=0.13.5
pyautogui>=0.9.54
pycaw>=20230407          # Windows volume control via WASAPI
comtypes>=1.2.0          # Required by pycaw (Windows COM interface)

# System info
psutil>=5.9.0

# Logging
colorama>=0.4.6
